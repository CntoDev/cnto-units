/*
 * attachments for weapons, the class names can be anything, but these
 * are the standard ones and they thus override anything previously defined
 */

#define optic(id) class LinkedItemsOptic { slot = "CowsSlot"; item = id; }
#define side(id) class LinkedItemsAcc { slot = "PointerSlot"; item = id; }
#define muzzle(id) class LinkedItemsMuzzle { slot = "MuzzleSlot"; item = id; }
#define under(id) class LinkedItemsUnder { slot = "UnderBarrelSlot"; item = id; }
